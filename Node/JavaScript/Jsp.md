# 点此回到[[JavaScript/开头]]

## 现在几乎不用，了解即可

## java server Page，java服务端页面，是servlet的动态页面的实现，本质是servlet类

## 9大内置对象
- request
- response
- pageContext
- session
- application
- config
- out
- page
- exception

## EL表达式
- ￥{EL表达式}，
- 支持内运算和判断
- 支持跳转
- 支持获取对象中的值

## 标签，c：if方式遍历，也是c:set来声明变量在作用域里

## cookie
- 不属于9大内置对象
- 该数据存储在浏览器中
- 只能存储字符串类型
- 是个对象，使用时要new对象，也是键值对的方式
- 

