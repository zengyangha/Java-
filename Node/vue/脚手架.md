# 点此回到[[vue/开头|开头]]

## 目录结构![[脚手架目录结构.PNG]]


## v-modle语法糖，双向绑定属性

## 组件中的data不能是对象，而是返回data对象的工厂模式函数

## 子组件不能直接修改父组件传递的数据，传递数据可用props，但其为只读属性，所以不能直接修改，而组件的data属性不能和prop属性重名

## vue是单向数据流，只能父传子，父可通过$emit来监听指定事件名




















