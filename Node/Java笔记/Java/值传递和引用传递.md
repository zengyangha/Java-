# 点此回到[[JAVA（由此开始）]]

## 值传递
- 在方法调用中，实参把值复制给形参，当数据量大时，占用内存空间，还会降低运行效率
- 即基本数据类型传值，对形参修改不会影响实参

## 引用传递
- 将对象地址传递，函数接收的是原始值的首地址，在方法的执行过程中，形参和实参内容相同，指向同个内存地址
- 而String、Integer、Float、Double等immutable的类型要特殊处理，传递的是复制的值，最后修改不会修改实参

































